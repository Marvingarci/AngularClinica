import { TelefonoemergenciaDirective } from './telefonoemergencia.directive';

describe('TelefonoemergenciaDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefonoemergenciaDirective();
    expect(directive).toBeTruthy();
  });
});
